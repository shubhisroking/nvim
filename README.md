```sh
rm -rf ~/.local/share/nvim && rm -rf ~/.local/state/nvim && rm -rf ~/.cache/nvim && rm -rf ~/.config/nvim && git clone https://github.com/shubhisroking/nvim.git "${XDG_CONFIG_HOME:-$HOME/.config}"/nvim
```
